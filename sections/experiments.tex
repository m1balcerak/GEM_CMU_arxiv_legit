\section{Experiments}
\label{sec:experiments}

% We use  two established molecular datasets QM9~\cite{ramakrishnan2014qm9} and MOSES~\cite{polykovskiy2020moses}. The QM9 is a widely used quantum-chemistry benchmark containing 133,885 small organic molecules with up to 9 heavy atoms (excluding hydrogens). MOSES is a large dataset consisting of $\sim$ 1.5M molecules heavily filtered to be drug discovery friendly. On these datasets we report(i) unconditional generation on MOSES and QM9, (ii) conditional generation (property optimization) and geodesic comparisons based on the learned potential on MOSES. Plwase note that the MOSES dataset provides a larger-scale benchmark ($\sim$1.5M molecules), demonstrating the scalability of GEM. 
We use two established molecular datasets, QM9~\cite{ramakrishnan2014qm9} and MOSES~\cite{polykovskiy2020moses}. QM9 comprises $\sim$150K small organic molecules containing up to nine heavy atoms (excluding hydrogens), whereas MOSES is a larger, drug-like dataset with roughly 1.5M molecules featuring up to 27 heavy atoms. We report (i) unconditional generation on both QM9 and MOSES, (ii) conditional generation (property optimization) on MOSES, and (iii) analyses of graph-to-graph geodesics on MOSES.

When applicable, we report (i) a fraction of \gls{vun} graphs (or \gls{vu} and \textit{Novelty} of the samples separately on QM9 as per~\citep{eijkelboom2024vfm}) and \gls{fcd}~\citep{preuer2018frechet}. For conditional tasks, we report \gls{cvun} i.e., a proportion of \gls{vun} graphs that simultaneously fulfill the prescribed property condition. We report results on two initialization regimes: noise initialization, where generation starts from easy-to-sample noise and must reach chemically valid molecules; and data initialization, applicable only to EBMs, which starts from training molecules and must produce novel molecules through edits.

% We report the well established metrics such novelty, validity and uniqueness
%
Baseline descriptions and experimental details are provided in \Cref{app:baselines} and \Cref{app:exp-setup}.
\input{figures/tex/fig_moses_vun_steps}


\subsection{Unconditional Generation}
\paragraph{MOSES.}
~\Cref{tab:uncond-main} (top) reports MOSES results. Across both noise and data initialization,
\gls{gem} outperforms baselines in terms of \gls{vun} and \gls{fcd}.
\Cref{fig:moses-vun-steps} contrasts performance versus inference steps and initialization strategies, respectively. 
At larger inference budgets, \gls{gem} substantially surpasses diffusion, yielding notably higher \gls{vun} and improved distributional fidelity (lower \gls{fcd}).
 % GEM demonstrates superior sample quality and diversity efficiency on MOSES under unconditional sampling.

\begin{table}[t]
\centering
\small
\captionsetup[subtable]{position=top,skip=3pt,font=bf}
\begin{subtable}[t]{\columnwidth}
\centering
\subcaption{MOSES ($\sim$1.5M molecules).}
\label{tab:uncond-moses}
\begin{tabular}{lcc}
\toprule
Method & \acrshort{vun} $\uparrow$ & \acrshort{fcd} $\downarrow$ \\
\midrule
Training samples (MOSES) & 0.0 & 0.25 \\
\midrule
\multicolumn{3}{l}{\textit{Noise Initialization}} \\
\midrule
GraphEBM$^*$~\citep{liu2021graphebm} & 0.081 & 9.83 \\
\gls{vfm}$^*$~\citep{eijkelboom2024vfm} & 0.814 & 2.71 \\
DeFoG$^*$~\citep{qinmadeira2024defog} & 0.822 & 1.95 \\
\textbf{\gls{gem} (Ours)} & \textbf{0.856} & \textbf{1.51} \\
\midrule
\multicolumn{3}{l}{\textit{Data Initialization}} \\
\midrule
GraphEBM$^*$~\citep{liu2021graphebm} & 0.322 & 7.31 \\
\textbf{\gls{gem} (Ours)} & \textbf{0.898} & \textbf{0.76} \\
\bottomrule
\end{tabular}
\end{subtable}
\par\addvspace{11pt}
\begin{subtable}[t]{\columnwidth}
\centering
\subcaption{QM9 ($\sim$0.15M molecules).}
\label{tab:uncond-qm9}
\begin{tabular}{lccc}
\toprule
Method & \glsentryshort{vu} $\uparrow$ & Novelty $\uparrow$ & \acrshort{fcd} $\downarrow$ \\
\midrule
Training samples (QM9) & 1.00 & 0.0 & 0.04 \\
\midrule
\multicolumn{4}{l}{\textit{Noise Initialization}} \\
\midrule
GraphEBM$^*$~\citep{liu2021graphebm} & 0.121 & 0.65 & 7.16 \\
\gls{vfm}~\citep{eijkelboom2024vfm} & \textbf{0.998} & 0.49 & \textbf{0.44} \\
DeFoG~\citep{qinmadeira2024defog} & 0.968 & 0.36 & 0.81 \\
\textbf{\gls{gem} (Ours)} & 0.971 & \textbf{0.63} & 0.92 \\
\midrule
\multicolumn{4}{l}{\textit{Data Initialization}} \\
\midrule
GraphEBM$^*$~\citep{liu2021graphebm} & 0.215 & 0.48 & 5.15 \\
\textbf{\gls{gem} (Ours)} & \textbf{0.985} & \textbf{0.51} & \textbf{0.31} \\
\bottomrule
\end{tabular}
\end{subtable}
\vspace{1em}
\caption{\footnotesize \textbf{Unconditional Generation.} Results grouped by initialization. MOSES
reports \acrshort{vun} and \acrshort{fcd} (lower is better); QM9 reports \gls{vu}, novelty,
and \acrshort{fcd}. $^*$ reproduced with the same backbone as DeFoG~\citep{qinmadeira2024defog}; \glspl{ebm} use
a small energy head. Architecture details are in \Cref{app:perm-invariance}. MOSES: 25k
generations, 1000 inference steps. QM9: 10k generations, 500 inference steps.}
\label{tab:uncond-main}
\end{table}

\paragraph{QM9.} ~\Cref{tab:uncond-main} (bottom) shows results on QM9. Under noise initialization, \gls{gem} achieves \gls{vu} scores comparable to discrete diffusion baselines, with performance close to saturation, while substantially surpassing them in novelty, indicating effective exploration from random initialization. It also clearly outperforms the previous graph-EBM approach. Under data initialization, \gls{gem} consistently achieves strong performance across all reported metrics.

\input{tables/tex/tab_moses_conditional}
\input{figures/tex/fig_mol_trajectories}
\input{figures/tex/fig_val_geodesics}
\subsection{Conditional Generation}
We conduct property optimization with respect to a diverse set of properties, including the logarithm of lipophilicity (logP), the logarithm of aqueous solubility (logS), \gls{qed}, and \gls{tpsa}. For comparability, we follow  ~\citep{vignac2022digress, ninniri2025graph}: we train a regressor \(f_\phi\) for differentiable guidance. For a desired property value $\zeta$ define \(V_\theta^{\mathrm{cond}}(x)=V_\theta(x)+\lambda_{\mathrm{prop}}\|f_\phi(x)-\zeta\|^2\), where $\lambda_{\mathrm{prop}}$ is the weighting for property criteria. Subsequently, we run the same proposal kernel $q_\theta(x\!\to\!y)$ using \(-\nabla_{\hat{x}} V_\theta^{\mathrm{cond}}(\hat{x})\),
with the \gls{mh} acceptance ratio modified accordingly. The above scheme can also be modified to accommodate a time- or noise-conditioned property regressor. For fairness, all methods in \Cref{tab:moses-conditional} use noise-conditioned regressors (see \Cref{app:regressor-noise}, \Cref{tab:regressor-ablations} for \gls{gem} ablations) and underwent comparable hyperparameter tuning procedures. We feed regressor time using an energy-based proxy that increases linearly until the chain reaches the data distribution energy level, after which it is clamped to $t=1$ (\Cref{app:regressor-noise}). Noise-conditioned regressors provide more reliable predictions on noisy/invalid graphs. The target molecular constraints (logS $\ge$ -2.25, logP $\le$ 1.5, \gls{qed} $\ge$ 0.9, \gls{tpsa} $\le$ 50) are selected such that unconditional MOSES samples satisfy each constraint approximately 20\% of the time, ensuring comparable difficulty across properties.
%  \Cref{tab:moses-conditional} reports the results:
% \gls{gem} maintains strong validity, uniqueness, and novelty while improving constraint satisfaction relative to baselines.
We observe \gls{gem} maintains strong validity, uniqueness, and novelty while improving constraint satisfaction relative to baselines (\Cref{tab:moses-conditional}).


\subsection{Geodesic Analysis}
% Here, we compute geodesics i.e., low-energy paths through graph edits aligned with the learned data geometry \citep{bethune2025follow} between molecules and compare against a cost-based interpolation baseline.
% We showcase average validity versus the endpoint distance induced by the node-matching approximation to
% $c_{\mathrm{FGW}}$ (~\Cref{app:matching}), see ~\Cref{fig:geodesic-validity}. Qualitative paths are shown in
% ~\Cref{fig:mol-trajectories}.
% Energy-weighted geodesics consistently preserve validity better than cost-only ones, indicating the energy captures chemically meaningful geometry. Experimental details, are provided in ~\Cref{app:geodesic-details}.
We study molecule-to-molecule interpolation by computing geodesics (low-energy graph-edit paths aligned with the learned data geometry \citep{bethune2025follow}). We compare these geodesics to a purely cost-based interpolation baseline. We report average validity versus the endpoint distance with distance computed via the node-matching approximation to $c_{\mathrm{FGW}}$ (\Cref{app:matching}). ~\Cref{fig:mol-trajectories} shows qualitative paths. As seen in \Cref{fig:geodesic-validity}, energy-weighted geodesics preserve validity more reliably than cost-only paths, indicating that the energy encodes chemically meaningful structure. ~\Cref{app:geodesic-details} provides experimental details. 


