\section{Experiments}
\label{sec:experiments}

We report unconditional generation on MOSES and QM9, conditional generation under property targets
(logP, logS, QED), and geodesic comparisons in the learned space. By convention on QM9 we report
novelty separately from $V.U.$: the dataset is small and novelty is often far from 1.0, so we include
a dedicated novelty column to show it is comparable and to avoid biasing FCD there.
We report validity (RDKit-sanitized non-empty SMILES), uniqueness (distinct SMILES among valid),
novelty (unique valid molecules not in the training split), and $V.U.N.$ for the fraction satisfying
all three. Sampling in discrete graph generation can start from \textbf{uniform noise} (node count
from the empirical histogram, node/edge types uniform), \textbf{marginal noise} (node count from the
empirical histogram, node/edge types from empirical marginals), or \textbf{training data} (samples
drawn from the training set), and can be run with diffusion/CTMC dynamics or MCMC.
MOSES is a large-scale molecular dataset (about 1.5M molecules), while QM9 is a smaller benchmark
focused on low-mass molecules. We use MOSES to stress-test sample quality at scale and QM9 as a
compact setting for comparison.
Experimental setup details are summarized in Appendix~\ref{app:exp-setup}.

\subsection*{Unconditional generation}
\paragraph{MOSES: Baselines vs.\ GEM.}
Table~\ref{tab:uncond} reports unconditional metrics on MOSES and QM9.
Figure~\ref{fig:moses-data-init-metrics} reports annealed-proposal metrics on MOSES, and
Figure~\ref{fig:moses-vun-steps} summarizes V.U.N.\ versus inference steps on MOSES.
Validity, novelty, and uniqueness can be improved post-hoc via rejection sampling, so diffusion
baselines in Table~\ref{tab:uncond} already reach strong V.U.(N). FCD is less amenable to such
correction; we see a clear gap on MOSES (and QM9).
\begin{table*}[t]
\centering
\small
\begin{minipage}{0.48\textwidth}
\centering
\textbf{MOSES} ($\sim$1.5M molecules)\\[2pt]
\begin{tabular}{lcc}
\toprule
Model & FCD $\downarrow$ & V.U.N. $\uparrow$ \\
\midrule
\multicolumn{3}{l}{\textit{Noise initialization (uniform or marginal)}} \\
\midrule
GraphEBM$^*$~\citep{liu2021graphebm} & 6.15 & 8.12 \\
VFM$^*$~\citep{eijkelboom2024vfm} & 2.15 & 81.68 \\
DeFoG$^*$~\citep{qinmadeira2024defog} & 1.95 & 82.21 \\
\textbf{GEM (Ours)} & X & X \\
\midrule
\multicolumn{3}{l}{\textit{Data initialization (editing)}} \\
\midrule
Training set (MOSES) & 0.09 & 0.0 \\
GraphEBM$^*$~\citep{liu2021graphebm} & 6.11 & 32.16 \\
\textbf{GEM (Ours)} & 0.76 & 89.81 \\
\bottomrule
\end{tabular}
\end{minipage}
\hfill
\begin{minipage}{0.48\textwidth}
\centering
\textbf{QM9} ($\sim$0.15M molecules)\\[2pt]
\begin{tabular}{lccc}
\toprule
Model & FCD $\downarrow$ & V.U. $\uparrow$ & Novelty $\uparrow$ \\
\midrule
\multicolumn{4}{l}{\textit{Noise initialization (uniform or marginal)}} \\
\midrule
GraphEBM~\citep{liu2021graphebm} & 7.16 & 8.07 & 0.62 \\
VFM~\citep{eijkelboom2024vfm} & 0.44 & 99.81 & 0.49 \\
DeFoG$^*$~\citep{qinmadeira2024defog} & 0.81 & 96.81 & 0.36 \\
\textbf{GEM (Ours)} & 0.92 & 97.11 & 0.63 \\
\midrule
\multicolumn{4}{l}{\textit{Data initialization (editing)}} \\
\midrule
Training set (QM9) & 0.04 & 100 & 0 \\
GraphEBM$^*$~\citep{liu2021graphebm} & 5.15 & 21.53 & 0.48 \\
\textbf{GEM (Ours)} & 0.31 & 98.51 & 0.51 \\
\bottomrule
\end{tabular}
\end{minipage}
\vspace{0.15cm}
\caption{\footnotesize Unconditional generation on MOSES and QM9, grouped by initialization. FCD is Fr\'echet ChemNet Distance (lower is better); $V.U.N.$ (MOSES) and $V.U.$ (QM9) are valid-unique-novel and valid-unique fractions (higher is better). $^*$ reproduced with the same backbone as DeFoG~\citep{qinmadeira2024defog}; EBMs use a small energy head. Architecture details are in Appendix~\ref{app:perm-invariance}. 10k generations. \textcolor{red}{[WiP]}}
\label{tab:uncond}
\end{table*}

A schematic of MCMC evolution for GEM on QM9 and MOSES is shown in Appendix~\ref{app:sampling}
(Figure~\ref{fig:appendix-mcmc-traj}).
Qualitative geodesic and linear trajectories on MOSES are shown in
Figure~\ref{fig:mol-trajectories}.

\begin{figure*}[t]
\centering
\begin{tikzpicture}[scale=1.15]
  % Layout constants
  \def\boxsize{1.4}
  \def\gap{2.4}
  \def\rowgap{1.8}
  \def\arrowlen{0.6}

  % Title
  \node[font=\small\bfseries] at (6.7,2.1) {MOSES Geodesics};

  % Row labels
  \node[anchor=east, align=center, font=\small\bfseries] at (-0.9,0.5*\boxsize) {$V_\theta(x)$-induced\\(GEM)};
  \node[anchor=east, font=\small\bfseries] at (-0.9,-\rowgap+0.5*\boxsize) {Linear};

  % Row 1 (QM9)
  \foreach \i/\lab in {0/A,1/B,2/C,3/D,4/E} {
    \draw[thick] (\i*\gap,0) rectangle ++(\boxsize,\boxsize);
    \node[font=\small\bfseries] at (\i*\gap+0.5*\boxsize,0.5*\boxsize) {\lab};
  }
  \foreach \i in {0,1,2,3} {
    \draw[->, thick]
      ({\i*\gap + 0.5*(\gap+\boxsize) - 0.5*\arrowlen}, {0.5*\boxsize})
      -- ({\i*\gap + 0.5*(\gap+\boxsize) + 0.5*\arrowlen}, {0.5*\boxsize});
  }
  % Row 2 (Linear)
  \foreach \i/\lab in {0/A,1/B,2/C,3/D,4/E} {
    \draw[thick] (\i*\gap,-\rowgap) rectangle ++(\boxsize,\boxsize);
    \node[font=\small\bfseries] at (\i*\gap+0.5*\boxsize,-\rowgap+0.5*\boxsize) {\lab};
  }
  \foreach \i in {0,1,2,3} {
    \draw[->, thick]
      ({\i*\gap + 0.5*(\gap+\boxsize) - 0.5*\arrowlen}, {-\rowgap+0.5*\boxsize})
      -- ({\i*\gap + 0.5*(\gap+\boxsize) + 0.5*\arrowlen}, {-\rowgap+0.5*\boxsize});
  }
\end{tikzpicture}
\caption{\footnotesize \textbf{Geodesic vs Linear Paths.} Molecule trajectories along continuous paths for MOSES. Top row: spline/geodesic path induced by $V_\theta(x)$ using the local metric $c_{\mathrm{loc}}$; bottom row: linear interpolation baseline. Boxes indicate samples along the continuous path. \textcolor{red}{[WiP]}}
\label{fig:mol-trajectories}
\end{figure*}

\begin{figure}[t]
\centering
\begin{tikzpicture}[scale=0.95]
  \draw[->, thick] (0,0) -- (7.2,0) node[midway, below=8pt] {MCMC steps};
  \draw[->, thick] (0,0) -- (0,3.2);
  \node[rotate=90] at (-0.9,1.6) {Metric};

  % Title
  \node[font=\small\bfseries] at (3.5,3.55) {Metrics for data-initialized samples};

  % Axes ticks
  \foreach \x/\lab in {0/0,1.4/100,2.8/200,4.2/300,5.6/400,7.0/500} {
    \draw (\x,0) -- (\x,-0.06);
    \node[below, font=\scriptsize] at (\x,-0.06) {\lab};
  }
  \foreach \y/\lab in {0/0.00,0.8/0.25,1.6/0.50,2.4/0.75,2.88/0.90,3.2/1.00} {
    \draw (0,\y) -- (-0.08,\y);
    \node[left, font=\scriptsize] at (-0.1,\y) {\lab};
  }

  % Reference line
  \draw[gray!55, dashed, line width=0.7pt] (0,2.88) -- (7.0,2.88);

  % Validity (green)
  \draw[green!70!black, thick] plot[smooth] coordinates {
    (0.0,0.32) (0.3,0.26) (0.6,0.22) (1.0,0.30) (1.4,0.42)
    (1.8,0.70) (2.2,1.05) (2.6,1.40) (3.0,1.70) (3.4,2.00)
    (3.8,2.25) (4.2,2.45) (4.6,2.60) (5.0,2.72) (5.6,2.86)
    (6.2,2.96) (6.8,3.02) (7.0,3.05)
  };

  % Uniqueness (deep purple, flat at 1.0)
  \draw[purple!90!black, thick] plot[smooth] coordinates {
    (0.0,3.18) (1.0,3.18) (2.0,3.18) (3.0,3.18)
    (4.0,3.18) (5.0,3.18) (6.0,3.18) (7.0,3.18)
  };

  % Novelty (brown)
  \draw[brown!80!black, thick] plot[smooth] coordinates {
    (0.0,2.55) (0.5,2.72) (1.0,2.45) (1.5,1.70) (2.0,2.05)
    (2.5,2.30) (3.0,2.45) (3.5,2.60) (4.0,2.70) (4.5,2.78)
    (5.0,2.85) (5.5,2.90) (6.0,2.95) (6.5,2.98) (7.0,3.00)
  };

  % Legend
  \begin{scope}[shift={(4.8,1.05)}]
    \draw[fill=white, draw=gray!35] (0,0) rectangle (2.0,1.1);
    \draw[green!70!black, thick] (0.15,0.85) -- (0.6,0.85);
    \node[anchor=west, font=\scriptsize] at (0.7,0.85) {validity};
    \draw[purple!90!black, thick] (0.15,0.55) -- (0.6,0.55);
    \node[anchor=west, font=\scriptsize] at (0.7,0.55) {uniqueness};
    \draw[brown!80!black, thick] (0.15,0.25) -- (0.6,0.25);
    \node[anchor=west, font=\scriptsize] at (0.7,0.25) {novelty};
  \end{scope}
\end{tikzpicture}
\caption{\footnotesize \textbf{Annealed Proposal Metrics (MOSES).} Metrics over MCMC steps with an annealed proposal ($S_{\mathrm{anneal}}=200$): validity rises with sampling, novelty rebounds, and uniqueness stays at 100\%. Dashed line indicates 0.90. \textcolor{red}{[WiP]}}
\label{fig:moses-data-init-metrics}
\end{figure}
Figure~\ref{fig:moses-vun-steps} summarizes V.U.N.\ versus inference steps on MOSES.

\begin{figure}[t]
\centering
\begin{tikzpicture}[scale=0.95]
  \draw[->, thick] (0,0) -- (6.2,0) node[midway, below=8pt] {inference steps};
  \draw[->, thick] (0,0) -- (0,3.2);
  \node[rotate=90] at (-0.95,1.6) {V.U.N.};

  % Title
  \node[font=\small\bfseries] at (3.1,3.55) {MOSES V.U.N. vs inference steps};

  % Axes ticks
  \foreach \x/\lab in {0/0,1.824/500,3.647/1000,5.471/1500,6.200/1700} {
    \draw (\x,0) -- (\x,-0.06);
    \node[below, font=\scriptsize] at (\x,-0.06) {\lab};
  }
  \foreach \y/\lab in {0/0.0,0.8/0.25,1.6/0.50,2.4/0.75,3.2/1.00} {
    \draw (0,\y) -- (-0.08,\y);
    \node[left, font=\scriptsize] at (-0.1,\y) {\lab};
  }
  % Best V.U.N. ticks
  \draw[blue!70!black] (0,2.861) -- (-0.08,2.861);
  \node[left, font=\scriptsize, text=blue!70!black, yshift=1pt] at (-0.1,2.861) {0.894};
  \draw[orange!90!black] (0,2.651) -- (-0.08,2.651);
  \node[left, font=\scriptsize, text=orange!90!black, yshift=-1pt] at (-0.1,2.651) {0.828};

  % Best V.U.N. references
  \draw[blue!70!black, dashed, line width=0.7pt] (0,2.861) -- (6.2,2.861);
  \draw[orange!90!black, dashed, line width=0.7pt] (0,2.651) -- (6.2,2.651);

  % GEM (blue)
  \draw[blue!70!black, thick] plot coordinates {
    (0.000,0.000) (0.036,0.505) (0.365,2.164) (1.094,2.680) (1.459,2.679)
    (1.824,2.710) (2.188,2.734) (2.553,2.783) (3.282,2.759) (4.012,2.773)
    (4.741,2.800) (5.471,2.826) (6.200,2.861)
  };

  % DeFoG (orange)
  \draw[orange!90!black, thick] plot coordinates {
    (0.036,0.102) (0.365,2.574) (1.094,2.631) (1.824,2.651)
    (2.553,2.649) (3.282,2.630) (4.012,2.629) (6.200,2.623)
  };

  % Legend
  \begin{scope}[shift={(3.75,0.6)}]
    \draw[fill=white, draw=gray!35] (0,0) rectangle (2.6,0.9);
    \draw[blue!70!black, thick] (0.15,0.62) -- (0.6,0.62);
    \node[anchor=west, font=\scriptsize] at (0.7,0.62) {GEM V.U.N. (15M params)};
    \draw[orange!90!black, thick] (0.15,0.32) -- (0.6,0.32);
    \node[anchor=west, font=\scriptsize] at (0.7,0.32) {DeFoG V.U.N. (15M params)};
  \end{scope}
\end{tikzpicture}
\caption{\footnotesize \textbf{V.U.N. vs Steps.} Fraction of valid, unique, and novel (V.U.N.) versus inference steps on MOSES, using 1k samples. DeFoG uses denoising steps and GEM uses MCMC steps. Dashed lines indicate best V.U.N.\ results for GEM and DeFoG. (Note to myself: its a normal proposal, try with annealed) \textcolor{red}{[WiP]}}
\label{fig:moses-vun-steps}
\end{figure}

\paragraph{QM9.}
QM9 unconditional metrics are reported in Table~\ref{tab:uncond}.

\subsection*{Conditional generation}
We evaluate conditional generation under molecular property constraints (e.g., logP, QED).
We train a property regressor \(f_\phi\) for differentiable guidance and define a conditioned
energy \(V_\theta^{\mathrm{cond}}(x)=V_\theta(x)+\lambda_{\mathrm{prop}}\|f_\phi(x)-y^\star\|^2\),
then run the same proposal kernel $q_\theta(x\!\to\!y)$ using \(-\nabla_x V_\theta^{\mathrm{cond}}(x)\),
with the Metropolis--Hastings acceptance ratio modified accordingly.
Table~\ref{tab:moses-conditional} reports single-property conditioning results, and
Table~\ref{tab:moses-conditional-joint} summarizes joint constraints. GEM maintains strong validity,
uniqueness, and novelty while improving constraint satisfaction relative to baselines.
GEM draws a candidate pool of size $N_{\mathrm{select}}$ from the training set, ranks by the
precomputed property values, and initializes the $B$ inference chains with the top-$B$ molecules
that satisfy the condition; this takes virtually no time because the training-set properties are
cached.

\begin{table*}[t]
\centering
\small
\begingroup
\setlength{\tabcolsep}{3.5pt}
\begin{tabular}{lcc cc cc}
\toprule
Model
& \multicolumn{2}{c}{Condition: logP $>$ 1.5}
& \multicolumn{2}{c}{Condition: logS $>$ 1.5}
& \multicolumn{2}{c}{Condition: QED $>$ 0.6} \\
\cmidrule(lr){2-3} \cmidrule(lr){4-5} \cmidrule(lr){6-7}
& C.V.U.N. $\uparrow$ & V.U.N. $\uparrow$
& C.V.U.N. $\uparrow$ & V.U.N. $\uparrow$
& C.V.U.N. $\uparrow$ & V.U.N. $\uparrow$ \\
\midrule
\multicolumn{7}{l}{\textit{Noise initialization (uniform or marginal)}} \\
\midrule
GraphEBM~\citep{liu2021graphebm} & X & X & X & X & X & X \\
G-VFM~\citep{eijkelboom2025controlledvfm} & 21.16 & 60.64 & X & X & X & X \\
DeFoG$^*$~\citep{qinmadeira2024defog} & 27.55 & 62.85 & X & X & X & X \\
\textbf{GEM (Ours)} & X & X & X & X & X & X \\
\midrule
\multicolumn{7}{l}{\textit{Data initialization (editing)}} \\
\midrule
Training set (MOSES) & 0 & 0 & 0 & 0 & 0 & 0 \\
GraphEBM~\citep{liu2021graphebm} & X & X & X & X & X & X \\
\textbf{GEM (Ours)} & \textbf{77.01} & \textbf{86.01} & \textbf{X} & \textbf{X} & \textbf{X} & \textbf{X} \\
\bottomrule
\end{tabular}
\endgroup
\vspace{0.15cm}
\caption{\footnotesize MOSES conditional generation under property constraints. $C.V.U.N.$ is the fraction meeting the condition and $V.U.N.$ is the valid-unique-novel marginal fraction. All entries are percentages. $V$, $U$, and $N$ denote validity, uniqueness, and novelty, respectively. $^*$ reproduced. \textcolor{red}{[WiP]}}
\label{tab:moses-conditional}
\end{table*}

\begin{table*}[t]
\centering
\small
\begingroup
\setlength{\tabcolsep}{3.5pt}
\begin{tabular}{lcccc}
\toprule
Model
& \multicolumn{2}{c}{\begin{tabular}{c}Joint condition: \\logP $>$ 1.5 \& logS $>$ 1.5\end{tabular}}
& \multicolumn{2}{c}{\begin{tabular}{c}Joint condition: \\logS $>$ 1.5 \& QED $>$ 0.6\end{tabular}} \\
\cmidrule(lr){2-3} \cmidrule(lr){4-5}
& C.V.U.N. $\uparrow$ & V.U.N. $\uparrow$
& C.V.U.N. $\uparrow$ & V.U.N. $\uparrow$ \\
\midrule
\multicolumn{5}{l}{\textit{Noise initialization (uniform or marginal)}} \\
\midrule
GraphEBM~\citep{liu2021graphebm} & X & X & X & X \\
G-VFM~\citep{eijkelboom2025controlledvfm} & X & X & X & X \\
DeFoG$^*$~\citep{qinmadeira2024defog} & X & X & X & X \\
\textbf{GEM (Ours)} & X & X & X & X \\
\midrule
\multicolumn{5}{l}{\textit{Data initialization (editing)}} \\
\midrule
Training set (MOSES) & 0 & 0 & 0 & 0 \\
GraphEBM~\citep{liu2021graphebm} & X & X & X & X \\
\textbf{GEM (Ours)} & \textbf{X} & \textbf{X} & \textbf{X} & \textbf{X} \\
\bottomrule
\end{tabular}
\endgroup
\vspace{0.15cm}
\caption{\footnotesize MOSES conditional generation with joint constraints on (logP, logS) and (logS, QED). All entries are percentages for $C.V.U.N.$ and $V.U.N.$. $^*$ reproduced. \textcolor{red}{[WiP]}}
\label{tab:moses-conditional-joint}
\end{table*}

\subsection*{Geodesic analysis}
We compute geodesics between molecules and compare against a linear interpolation baseline.
All alignments are within fixed node counts, using node-matching to define couplings between pairs.
We sample 1000 molecules, form same-size pairs, and for each pair construct (i) an energy-weighted
geodesic and (ii) a linear interpolation along the coupling. From the continuous path we obtain
discrete graphs via categorical sampling, then compute the average validity along each path.
We plot average validity versus the endpoint distance induced by the matching cost
$c_{\mathrm{MongeFGW}}$ (node-matching, Appendix~\ref{app:matching}), comparing geodesic vs.\ linear;
see Figure~\ref{fig:geodesic-validity}. Qualitative paths are shown in
Figure~\ref{fig:mol-trajectories}.
We report the length ratio $L_{\mathrm{geo}}/L_{\mathrm{lin}}$ and the correlation between energy and
validity along the path; we observe higher validity, a shorter length ratio, and a strongly negative
energy--validity correlation.
We implement the geodesic path as a spline (Bezier) interpolation over node/edge logits in the
probability simplex, then sample discrete graphs along evenly spaced points on the path. The linear
baseline uses the same endpoints and sampling protocol. Appendix~\ref{app:geodesic-details} provides
the implementation details.

In the embedding space we consider paths $\hat{\gamma}(t)$ and use the local geometry induced by
\eqref{eq:loc-cost}. Define
\begin{equation}
\|\dot{\hat{\gamma}}(t)\|_{\mathrm{loc}}^2
=\lambda_X\sum_{i\in\mathcal{V}}\|\dot{\hat{\gamma}}_i(t)\|^2
+\lambda_E\sum_{i<j}\|\dot{\hat{\gamma}}_{ij}(t)\|^2,
\end{equation}
and the energy-weighted length
\begin{equation}
L_\theta(\gamma)=\int_0^1 \exp\!\big(-\beta V_\theta(\hat{\gamma}(t))\big)\,
\|\dot{\hat{\gamma}}(t)\|_{\mathrm{loc}}\,\mathrm{d}t.
\end{equation}
We optimize the geodesic objective
\begin{equation}
\mathcal{J}_{\mathrm{geo}}(\gamma)
=
L_\theta(\gamma)
\;+\lambda_{ME}\int_0^1 V_\theta(\hat{\gamma}(t))\,\mathrm{d}t,
\end{equation}
Additional details are in Appendix~\ref{app:geodesic-details}.

\begin{figure}[t]
\centering
\begin{tikzpicture}[scale=0.95]
  \draw[->, thick] (0,0) -- (5.2,0) node[below, xshift=-1.5cm] {endpoint distance (cost-induced)};
  \draw[->, thick] (0,0) -- (0,3.2);
  \node[rotate=90] at (-0.6,1.6) {avg.\ validity (\%)};

  \draw (0,0) -- (-0.08,0);
  \node[left, font=\scriptsize] at (-0.1,0) {0};
  \draw (0,3.2) -- (-0.08,3.2);
  \node[left, font=\scriptsize] at (-0.1,3.2) {100};

  \draw[gray!60, dashed] (0,0) -- (5.2,0);
  \draw[gray!60, dashed] (0,3.2) -- (5.2,3.2);

  \draw[thick, blue] plot[smooth] coordinates {(0.0,2.8) (1.2,2.5) (2.2,2.0) (3.2,1.6) (4.2,1.3)};
  \draw[thick, gray, dashed] plot[smooth] coordinates {(0.0,1.1) (1.2,0.9) (2.2,0.7) (3.2,0.6) (4.2,0.5)};

  \node[blue, anchor=west] at (3.1,2.5) {geodesic};
  \node[gray, anchor=west] at (3.1,2.0) {linear};
\end{tikzpicture}
\caption{\footnotesize \textbf{Geodesic Validity.} Expected average validity vs.\ endpoint distance (cost-induced) for geodesic vs.\ linear interpolation. \textcolor{red}{[WiP]}}
\label{fig:geodesic-validity}
\end{figure}
